import arc.*;

public class repotest{
	public static void main (String[] args){
			String strMovies[][];
			
			strMovies 
			
			
			
			strMove[1][0]
			
			
			strMovie[3][1] = "69";
			strMovie[3][2] = "93";

			strMovie[4][0] = "Brutalist";
			strMovie[4][1] = "93";
			strMovie[4][2] = "80";

			// print out array
			int intCount;
			con.println("Before sorting");
			for(intCount = 0; intCount < 5; intCount++) {
				con.println(strMovie[intCount][0] + " - " + strMovie[intCount][1] + " - " + strMovie[intCount][2]);
			}

			int intCount2;
			String strNameTemp;
			String strCriticsTemp;
			String strPeopleTemp;

			for(intCount = 0; intCount < 5-1; intCount++) {
				if(Integer.parseInt(strMovie[intCount][1]) > Integer.parseInt(strMovie[intCount+1][1])){
					
					
					strNameTemp = strMovie[intCount][0];
					strMovie[intCount][0] = strMovie[intCount+1][0]
					strMovie[intCount+1][0] = strNameTemp;
					
					strCriticsTemp = strMovie[intCount[1];
					strMovie[intCount[1] = strMovie[intCount+1][1];
					strMovie[intCount+1][1] = strCriticsTemp;
					
					streopleTemp = strMovie[intCount
    )
    }

    con.println("\n\nAfter sorting");
    for(intCount = 0; intCount < 5; intCount++) {
    con.println(strMovie[intCount][0] + " - " + strMovie[intCount][1] + " - " + strMovie[intCount][2]);
    }
}

//Users/acadams/Desktop/Coup Sci lib/src.jar:

			
			
			
			
	}
}
